// examples/demo.ltl — runnable demo for ltl_csp_demo

import std::io
use ltl_csp_demo as lib

fn main() {
    io::println("ltl_csp_demo demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}